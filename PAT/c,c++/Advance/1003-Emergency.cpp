#include <iostream>
#include <vector>
#include <stack>
#include <queue>

using namespace std;

int main(void)
{
        struct {
                int city, road;
        }amount;
        int root, endPoint;

        cin >> amount.city >> amount.road >> root >> endPoint;

        int *containTeam = new int[amountcity];
        for(auto i = 0; i < amount.city; i++)
        {
                cin >> containTeam[i];
        }

        vector<int, int>



        return 0;
}
